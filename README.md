# Python Grading System

A simple Python program that takes student marks (0–100) and returns a grade based on performance.  
It includes input validation and outputs a descriptive evaluation for each grade.

## 📝 Features

- Validates user input (prevents non-numeric entries)
- Handles marks from **0 to 100**
- Uses clean grading logic with `if/elif`
- Provides meaningful descriptions for each grade
- Simple, beginner-friendly Python code

